require 'spec_helper'

describe I18n::Cocoa do
  it 'has a version number' do
    expect(I18n::Cocoa::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
